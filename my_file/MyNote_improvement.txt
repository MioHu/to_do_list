1. 成功发送分享的提醒框
try: Bootstrap Toast, failed because of redirect

2. 共享者与被共享者区别（功能，显示…），罗列共享者
thought:
  lists table: share 1/0
  share_add: SET list.share=1
  'list/id': if list.share==1  sharer, member
  html: if session[uid']==sharer.id 
           else  no edit, delete, share

3. AWS

4. 拖动排序list

5. js实现task排序

6.AJAX 不加载页面交换数据、更新内容